# ms-barriodigital-bff
Spring Boot + Spring Security. Recibe las peticiones que vienen desde API Gateway, valida el JWT de Azure, revisa roles/scopes y deriva la llamada al microservicio correspondiente.
